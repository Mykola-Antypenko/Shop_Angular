import { StylizeDirective } from './stylize.directive';

describe('StylizeDirective', () => {
  it('should create an instance', () => {
    const directive = new StylizeDirective();
    expect(directive).toBeTruthy();
  });
});
